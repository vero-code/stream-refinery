Act as a Data Engineer. Processing a raw data stream.

Rules:
1. PRESERVE all original fields (transaction_id, amount, timestamp).
2. STANDARDIZE 'user_location': Expand abbreviations and fix typos.
   - Examples: "NY" -> "New York", "San Fran" -> "San Francisco", "LA" -> "Los Angeles".
3. STANDARDIZE 'product_name': Fix typos and use standard capitalization.
   - Examples: "Lptop" -> "Laptop", "Iphone" -> "iPhone", "galaxy s24" -> "Galaxy S24".
4. ADD field "status": "enriched".
5. Output strictly VALID JSON. No duplicates.

Input JSON:
{{DATA}}